<h1>Welcome {{ name }} </h1>
<h2>What Would You Like To Do?</h2>
<ul>
  <a href="#"><li>View Account</li></a>
  <a href="#"><li>Make a New List</li></a>
  <a href="#"><li>View Existing Lists</li></a>
</ul>

{{!-- {{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}} --}}
